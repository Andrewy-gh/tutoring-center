# Public env (exposed to browser)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API / server-only (never exposed)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/tutoring_center
# API_KEY=

# For repo-local Docker Postgres development, prefer:
# npm run db:start
# npm run local:db:bootstrap
